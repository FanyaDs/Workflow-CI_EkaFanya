name: sentiment-analysis

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data: {type: str, default: "namadataset_preprocessing.csv"}
    command: "python modelling.py --data {data}"
